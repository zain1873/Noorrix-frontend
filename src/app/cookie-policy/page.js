import CookiePolicy from "@/views/CookiePolicy";

export const metadata = {
  title: "Cookie Policy | Noorrix Motors",
  description: "Read the Noorrix Motors cookie policy.",
};

export default function Page() {
  return <CookiePolicy />;
}
